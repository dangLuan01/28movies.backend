services:
  app:
    build:
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel_app
    working_dir: /app
    # volumes:
    #   - ./28movies.backend:/app
    env_file:
      - .env
    ports:
      - "3000:9000"
    networks:
      - app-network
  queue:
    image: laravel-app
    container_name: laravel_queue
    working_dir: /app
    command: php artisan queue:work --daemon --sleep=3 --tries=3 --timeout=60
    env_file:
      - .env
    restart: always
    networks:
      - app-network
networks:
  app-network:
    external: true
    name: app-network